// UI Components - Reusable interface elements

// Common UI components would go here when created
// Examples: Button, Input, Modal, LoadingSpinner, etc.

// For now, we can export any existing reusable components
export { default as QRDisplay } from '../common/QRDisplay'